set kth bit
ay position lo niku 1 kavali o 1<<k chesinapudu akkada one ochi migithavi ani zeros ayipothai
so epudu ayithe ichina n value ni e 1<<k ni (or) chesthavo niku a place lo one osthadi 

class Solution:
	def setKthBit(self, n, k):
		return n| (1<<k)

check k-th bit 
kth position lo 1 unte True 
else false
class Solution:
    def checkKthBit(self, n, k):
        if n & (1<<k):
            return True
        else:
            return False
toggle bits in given range
class Solution:
    def toggleBits(self, n , l , r):
        mask=((1 << (r - l + 1)) - 1) << (l - 1)
        return  n ^ mask
generate subsets using bit manuplation
no of subsets for length is 2^n daniki formula 1<<len(arr)
n=[1,2,3]
ans=[]
for val in range(1<<len(n)):
    temp=[]
    for i in range(len(n)):
        if val & (1<<i):
            temp.append(n[i])
    ans.append(temp)
print(ans)
